fileVersion: 1
id: 16c4eb34-24f7-4244-8697-274c70d4dda4
name: V_NODE_GROWTH_MEDIANS
operation:
  config:
    OverrideSQLCreate: false
    groupByAll: false
    insertStrategy: UNION
    selectDistinct: false
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: TARGET
  materializationType: view
  metadata:
    appliedNodeTests: []
    columns:
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: ee54ed95-4725-41d6-9437-3d94f907d63f
          stepCounter: 16c4eb34-24f7-4244-8697-274c70d4dda4
        config: {}
        dataType: string
        defaultValue: ""
        description: ""
        keyColumnType: None
        name: metric_name
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
        systemColumnType: None
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: e42ba6a9-6d8f-4574-80cd-2c5a2e8cf944
          stepCounter: 16c4eb34-24f7-4244-8697-274c70d4dda4
        config: {}
        dataType: string
        defaultValue: ""
        description: ""
        keyColumnType: None
        name: arr_cohort
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
        systemColumnType: None
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: 2325a63e-473c-40ce-8331-79d907d87940
          stepCounter: 16c4eb34-24f7-4244-8697-274c70d4dda4
        config: {}
        dataType: double
        defaultValue: ""
        description: ""
        keyColumnType: None
        name: metric_median
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
        systemColumnType: None
    cteString: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases: {}
        customSQL:
          customSQL: |-
            {{ stage('Override Create SQL') }}
             CREATE OR REPLACE VIEW {{ ref('TARGET', 'V_NODE_GROWTH_MEDIANS')}} AS (
            with `source_100K+` as (
            select *
            from customer_health_metrics.default.fct_node_growth_rate
            where account_tenure > 6
            and node_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and node_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '100K+'
            ),


            `100K+` as (
            select
             'node_growth_rate' as metric_name, 
             '100K+' as arr_cohort,
             median(distinct node_growth_rate) as metric_median
            from
             `source_100K+`
            group by all
            ),


            `source_50K-100K` as (
            select *
            from customer_health_metrics.default.fct_node_growth_rate
            where account_tenure > 6
            and node_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and node_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '50K-100K'
            ),


            `50K-100K` as (
            select
             'node_growth_rate' as metric_name, 
             '50K-100K' as arr_cohort,
             median(distinct node_growth_rate) as metric_median
            from
             `source_50K-100K`
            group by all
            ),


            `source_30K-50K` as (
            select *
            from customer_health_metrics.default.fct_node_growth_rate
            where account_tenure > 6
            and node_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and node_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '30K-50K'
            ),


            `30K-50K` as (
            select
             'node_growth_rate' as metric_name, 
             '30K-50K' as arr_cohort,
             median(distinct node_growth_rate) as metric_median
            from
             `source_30K-50K`
            group by all
            ),


            `source_0-30K` as (
            select *
            from customer_health_metrics.default.fct_node_growth_rate
            where account_tenure > 6
            and node_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and node_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '0-30K'
            ),


            `0-30K` as (
            select
             'node_growth_rate' as metric_name, 
             '0-30K' as arr_cohort,
             median(distinct node_growth_rate) as metric_median
            from
             `source_0-30K`
            group by all
            )


            select *
            from `100K+`
            UNION
            select *
            from `50K-100K`
            UNION
            select *
            from `30K-50K`
            UNION
            select *
            from `0-30K`)
        dependencies:
          - locationName: TARGET
            nodeName: FCT_NODE_GROWTH_RATE
        join:
          joinCondition: FROM {{ ref('TARGET', 'FCT_NODE_GROWTH_RATE') }} `FCT_NODE_GROWTH_RATE`
        name: V_NODE_GROWTH_MEDIANS
        noLinkRefs: []
  name: V_NODE_GROWTH_MEDIANS
  overrideSQL: true
  schema: ""
  sqlType: View
  type: sql
  version: 1
type: Node
