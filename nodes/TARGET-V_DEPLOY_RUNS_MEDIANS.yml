fileVersion: 1
id: a44090b1-a226-4c0e-ad3f-eb5d246eacc1
name: V_DEPLOY_RUNS_MEDIANS
operation:
  config:
    OverrideSQLCreate: false
    groupByAll: false
    insertStrategy: UNION
    selectDistinct: false
  database: ""
  deployEnabled: true
  description: ""
  isMultisource: false
  locationName: TARGET
  materializationType: view
  metadata:
    appliedNodeTests: []
    columns:
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: 179359ab-bee0-4554-bf9d-f86e091e77de
          stepCounter: a44090b1-a226-4c0e-ad3f-eb5d246eacc1
        config: {}
        dataType: string
        defaultValue: ""
        description: ""
        keyColumnType: None
        name: metric_name
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
        systemColumnType: None
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: f0ba3741-cc92-4352-a641-f39f789f5e60
          stepCounter: a44090b1-a226-4c0e-ad3f-eb5d246eacc1
        config: {}
        dataType: string
        defaultValue: ""
        description: ""
        keyColumnType: None
        name: arr_cohort
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
        systemColumnType: None
      - acceptedValues:
          strictMatch: true
          values: []
        appliedColumnTests: {}
        columnReference:
          columnCounter: 8aad7217-0fd4-4573-9530-bb6952542a0a
          stepCounter: a44090b1-a226-4c0e-ad3f-eb5d246eacc1
        config: {}
        dataType: double
        defaultValue: ""
        description: ""
        keyColumnType: None
        name: metric_median
        nullable: true
        sourceColumnReferences:
          - columnReferences:
              - columnCounter: "0"
                stepCounter: "0"
            transform: ""
        systemColumnType: None
    cteString: ""
    enabledColumnTestIDs: []
    sourceMapping:
      - aliases: {}
        customSQL:
          customSQL: |
            {{ stage('Override Create SQL') }}
             CREATE OR REPLACE VIEW {{ ref('TARGET', 'V_DEPLOY_RUNS_MEDIANS')}} AS (
            with `source_100K+` as (
            select *
            from customer_health_metrics.default.fct_monthly_deploy_runs
            where account_tenure > 6
            and run_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and run_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '100K+'
            ),


            `100K+` as (
            select
             'monthly_deploys' as metric_name, 
             '100K+' as arr_cohort,
             median(distinct total_deploy_runs) as metric_median
            from
             `source_100K+`
            group by all
            ),


            `source_50K-100K` as (
            select *
            from customer_health_metrics.default.fct_monthly_deploy_runs
            where account_tenure > 6
            and run_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and run_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '50K-100K'
            ),


            `50K-100K` as (
            select
             'monthly_deploys' as metric_name, 
             '50K-100K' as arr_cohort,
             median(distinct total_deploy_runs) as metric_median
            from
             `source_50K-100K`
            group by all
            ),


            `source_30K-50K` as (
            select *
            from customer_health_metrics.default.fct_monthly_deploy_runs
            where account_tenure > 6
            and run_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and run_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '30K-50K'
            ),


            `30K-50K` as (
            select
             'monthly_deploys' as metric_name, 
             '30K-50K' as arr_cohort,
             median(distinct total_deploy_runs) as metric_median
            from
             `source_30K-50K`
            group by all
            ),


            `source_0-30K` as (
            select *
            from customer_health_metrics.default.fct_monthly_deploy_runs
            where account_tenure > 6
            and run_month >= date_add(month,-12,DATE_TRUNC('month',GETDATE()))
            and run_month < DATE_TRUNC('month',GETDATE())
            and arr_cohort = '0-30K'
            ),


            `0-30K` as (
            select
             'monthly_deploys' as metric_name, 
             '0-30K' as arr_cohort,
             median(distinct total_deploy_runs) as metric_median
            from
             `source_0-30K`
            group by all
            )


            select *
            from `100K+`
            UNION
            select *
            from `50K-100K`
            UNION
            select *
            from `30K-50K`
            UNION
            select *
            from `0-30K`)
        dependencies:
          - locationName: TARGET
            nodeName: FCT_MONTHLY_DEPLOY_RUNS
        join:
          joinCondition: FROM {{ ref('TARGET', 'FCT_MONTHLY_DEPLOY_RUNS') }} `FCT_MONTHLY_DEPLOY_RUNS`
        name: V_DEPLOY_RUNS_MEDIANS
        noLinkRefs: []
  name: V_DEPLOY_RUNS_MEDIANS
  overrideSQL: true
  schema: ""
  sqlType: View
  type: sql
  version: 1
type: Node
